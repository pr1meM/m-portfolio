---
title: "useful commands"
---

> Placeholder page — replace with real notes.

```bash
# quick python http server
python3 -m http.server 8000

# find suid binaries
find / -perm -4000 -type f 2>/dev/null

# strings with context
strings -a -t x ./binary | less
```
